CHANGELOG — Plataforma de CV + Asistente IA "Jack"
Repo: https://github.com/jacklanders/cv_ai_proyect

Formato: cada versión documenta los cambios antes de avanzar a la siguiente.

--------------------------------------------------------------------
v1.0 — Fase 1 (maqueta, sin backend)
--------------------------------------------------------------------
- Sistema de diseño: paleta teal-ink / crema / ámbar, tipografías Space Grotesk
  (títulos) y DM Sans (texto), definido en src/styles.css.
- Componentes base: SiteHeader (público) y AppShell (área logueada con menú
  lateral y contador de uso diario de IA).
- Rutas creadas:
  * /        landing con los 3 pasos (subir, mejorar, exportar).
  * /login   pantalla partida con acceso Google y mail.
  * /perfil  datos personales, resumen profesional, skills y firma de mail,
             con barra de completitud.
  * /cv      editor de CV con pestañas Editar / Vista previa y chat lateral
             con Jack para sugerencias por sección.
  * /mis-cv  versiones guardadas, duplicar, editar y exportar a PDF con
             selector de plantilla (Clásica, Moderna, ATS puro).
- Todo con datos simulados: sin login real, sin base de datos, sin API de IA.

--------------------------------------------------------------------
v2.0 — Fase 2 (maqueta): postulaciones y envío de mail
--------------------------------------------------------------------
- Nuevo módulo de datos simulados: src/lib/mock-postulaciones.ts (estado en
  memoria, pensado para reemplazarse luego por la base de datos sin reescribir
  las pantallas).
- Rutas creadas:
  * /postulaciones         historial de vacantes con estado Enviada /
                           Descartada (con motivo) / Pendiente, filtros por
                           estado y buscador.
  * /postulaciones/nueva   carga del aviso por texto pegado o imagen
                           (drag & drop), con extracción simulada de datos.
  * /postulaciones/$id     generador de mail: selector de asunto (genérico de
                           Jack vs. asunto obligatorio del aviso), campos
                           origen fijo / destino / cuerpo / CCO con botón de
                           copiar individual, adjunto de CV, firma editable y
                           envío simulado.
- Reglas de Jack reflejadas en la interfaz: alerta por requisito excluyente no
  cumplido, preguntas cuando falta información, aviso de vacante vencida.
- Límite freemium: 2 mails de postulación por día, con estado de límite
  alcanzado (sin cobro real).
- AppShell: nueva entrada "Postulaciones" en el menú y contador de mails del
  día; enlace desde Mis CVs para usar una versión en una postulación.

--------------------------------------------------------------------
v3.0 — Hito 1: login real, rutas protegidas y UI conectada a Supabase
--------------------------------------------------------------------
- Infraestructura de auth:
  * src/lib/supabase/client.ts — cliente de navegador tipado con manejo
    defensivo de variables faltantes.
  * src/lib/supabase/auth-middleware.ts — requireSupabaseAuth que valida
    bearer token y ejecuta queries con RLS como el usuario autenticado.
  * src/lib/supabase/auth-attacher.ts — middleware de cliente que adjunta
    automáticamente el access token a cada server function.
  * src/hooks/useAuth.ts — sesión, usuario, signOut y helpers de UI.
  * src/routes/_authenticated/route.tsx — layout protegido (ssr: false) que
    redirige a /login si no hay sesión.
  * src/routes/login.tsx — login real con Google (signInWithOAuth), login por
    email, redirect de retorno y aviso de configuración faltante.
  * src/components/UserMenu.tsx — menú de usuario con avatar, iniciales y
    cierre de sesión integrado en AppShell.
  * src/start.ts — registrado attachSupabaseAuth en functionMiddleware.
- Datos reales:
  * src/lib/perfil.model.ts + src/lib/perfil.functions.ts — getMiPerfil
    (alta automática en primer login) y guardarPerfil sobre la tabla profiles.
  * src/lib/cv.model.ts + src/lib/cv.functions.ts — listar, crear, duplicar,
    borrar, leer por ID y guardar resumes en Supabase.
  * /perfil, /mis-cv y /cv reescritos para usar useServerFn + TanStack Query,
    reemplazando los datos mockeados.
- Tests: unitarios para perfil.model y cv.model en src/lib/__tests__.
- /postulaciones/* sigue con datos simulados; se conecta en el Hito 2.

Pendiente de integración:
- Aplicar supabase/migrations/0001_init.sql en el proyecto Supabase Cloud.
- Cargar VITE_SUPABASE_URL y VITE_SUPABASE_ANON_KEY (y la configuración de
  Google OAuth en Supabase) para que el login real funcione en preview/producción.
- Integrar el patch de backend de Claude que usa cookies @supabase/ssr si se
  decide migrar de bearer tokens a ese modelo de sesión.
- Tests E2E de login real una vez cargadas las credenciales (los tests E2E
  mockeados existentes solo validan la UI y los contratos de integración).

